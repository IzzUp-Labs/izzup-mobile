import 'package:flutter/material.dart';

class IdUnverifiedPopup extends StatefulWidget {
  const IdUnverifiedPopup({Key? key}) : super(key: key);

  @override
  State<IdUnverifiedPopup> createState() => _IdUnverifiedPopupState();
}

class _IdUnverifiedPopupState extends State<IdUnverifiedPopup> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
